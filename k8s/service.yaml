apiVersion: v1
kind: Service

metadata:
  name: flask-devops-service

spec:
  type: LoadBalancer

  selector:
    app: flask-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000